; Стартовый код порта.  .rom/.com Вектора грузится и стартует с 0x0100,
; а по 0x02A0 лежит мёртвая голова стадии 2 (вызовы BIOS MSX), исполнять
; её нельзя.  Здесь только передача управления адаптеру экрана: он ставит
; палитру, чистит плоскости, патчит резидентный слой и уходит в игровой
; цикл 0x0489.  Ввод и звук ставит свой init — ему место тут же.
                org     0x0100
                di
                ld      sp,0x6500      ; вершина стека, docs/v06-memory.md §8
                jp      0x5000          ; SCR_INIT экранного адаптера
